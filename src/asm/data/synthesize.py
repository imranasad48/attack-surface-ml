"""Generate a synthetic asset inventory for development.

Real attack surface scanners are out of scope for solo work. Synthetic data
keeps the pipeline runnable without requiring infrastructure to scan.
"""
# TODO: emit ~5-10k assets with services, versions, exposure flags.
